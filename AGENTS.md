# Agent context

See **`CLAUDE.md`** in this same directory for the full working context: what this tree contains, the conda environments,
where the data and the GCS archive live, how the two pipeline modules are run, the conventions this project follows, and the
mistakes already paid for. The resume point for the work is `asc/nisar_workflows/STATE.md`.
